## 1.6
- Display just "0" x or y coordinate instead of "-0" in some rare cases.

## 1.5
- Show just one coordinate for two collapsed (or very close) nodes instead of hiding both.

## 1.4
- Selected nodes limitation lifted. Old behaviour — show coordinates for only 1 selected node or segment. New behaviour — show coordinates (and distances and angles) for all selected nodes.

## 1.3
- Show segment intersections (shapes overlap or open corners) as separate distances.

## 1.2
- Visual and positioning improvements. The colors of distance and angle in Light mode are more contrasted now.

## 1.1
- Dark Mode detection added.
